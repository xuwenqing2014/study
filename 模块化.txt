1、commonjs
commonjs规范使用于服务端，例如nodejs，加载是同步的，要等所有的依赖都加载完之后才会继续执行
2、AMD，代表是requirejs
AMD的加载是异步的，后面的操作会放进回调函数里面去，依赖前置，需要用的依赖先引入，浏览器端使用
3、CMD，代表是seajs
CMD的加载是异步的，就近依赖，哪里需要用到就在哪引入，浏览器端使用
4、ES6模块化
浏览器和服务端都可以使用